{
  "name": "cible-zklogin-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "Stateless bridge that performs the cryptographic operations iOS Swift can't do natively for zkLogin: address derivation (Poseidon + Blake2b), signature wrapping (BCS), PTB byte construction, and sponsored-tx co-signing.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@mysten/sui": "1.38.0",
    "@mysten/zklogin": "0.7.30",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
