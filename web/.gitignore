node_modules
.next
out
.env
.env.local
.env*.local
.env.vercel
**/.env.vercel
.data
*.tsbuildinfo
.DS_Store
/public/shield/
# Shielded-pool lifecycle prover artifact — holds note secrets, never commit.
.shield-lifecycle-art.json
**/.shield-lifecycle-art.json
.vercel

# Credentials — never commit signing keys / certs / local secret stores.
*.p8
*.p12
*.pem
*.key
.secrets/
